Time complexity is O(n), memory complexity O(n) for stack of elements.
Stack is created, if element is open parenthesis, then it is pushed on the stack. If element is closing parenthesis, it should be matching top element of stack and then top element can be popped. Otherwise, false returned.